services:
  - type: web
    name: kaamsetu-api
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: PLATFORM_FEE_PERCENT
        value: "10"
      - key: PAYMENT_MODE
        value: "sandbox"
